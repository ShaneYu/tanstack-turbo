{
  "name": "@repo/api",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./index.js",
    "./client": "./src/client.ts"
  },
  "scripts": {
    "dev": "dotenv -e .env -- vite dev --port 3001",
    "build": "vite build",
    "check-types": "tsc --noEmit",
    "start": "echo \"start script not setup yet\" && exit 1",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/node-server": "catalog:build",
    "@hono/zod-validator": "catalog:",
    "@repo/auth": "workspace:*",
    "@repo/db": "workspace:*",
    "@repo/validation": "workspace:*",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@hono/vite-build": "catalog:build",
    "@hono/vite-dev-server": "catalog:build",
    "@repo/biome-config": "workspace:*",
    "@repo/tsconfig": "workspace:*",
    "@repo/vitest-config": "workspace:*",
    "@types/bun": "catalog:",
    "@vitest/coverage-istanbul": "catalog:testing",
    "dotenv-cli": "catalog:build",
    "typescript": "catalog:",
    "vite": "catalog:build",
    "vitest": "catalog:testing"
  }
}
